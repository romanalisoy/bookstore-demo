export interface Book {
    id?: number;
    title: string;
    author: string,
    published_date: string
    isbn: string,
    number_of_pages: number,
    cover_image_url: string,
    language: string,
}

